enum EmailType {
  PASSWORD
}

type SendEmailResult {
    isSuccess: Boolean
}

type mutation{
   sendEmail(toAddress: String, type: EmailType): SendEmailResult
}

type Query {
    shows(titleFilter: String): [Show]
}

type Show {
    title: String
    releaseYear: Int
}